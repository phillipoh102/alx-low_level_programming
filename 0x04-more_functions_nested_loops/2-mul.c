#include <stdio.h>

/**
 * mul() - Function to multiply two integers
 * @a: First integer
 * @b: Second integer
 *
 * Return: The product of the two integers
 */
int mul(int a, int b)
{
    int result;
    result = a * b;
    return result;
}